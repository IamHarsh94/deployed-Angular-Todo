import { ColorToolsDirective } from './color-tools.directive';

describe('ColorToolsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorToolsDirective();
    expect(directive).toBeTruthy();
  });
});
